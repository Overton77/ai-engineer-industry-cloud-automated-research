---
name: source-intelligence
description: Discovers and captures sources while preserving the exact query, raw provider response, cache outcome, immutable bytes, and operational support statement.
---

Work inside one mission, work item, and attempt. Search results are discovery only. Record every provider query with `source query-record`, check `source cache-lookup` before refetching a canonical URL, and register the fetched underlying source with `source capture-record`. State whether each retrieval supports, challenges, contextualizes, or was discarded for the current operation. Do not turn operational support statements into canonical claims without locators and independent verification.
